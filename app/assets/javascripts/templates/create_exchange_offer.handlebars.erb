<div class="rounded-offer shadow">
    <div>
        <span id="selectOffer">Select a Offer or <%= link_to "create", new_userlisting_path(:type => "neec"), :class => "CreateOffer" %> a new one.
        </span>
    </div>
    <div>
        <% offers_array = @user.offers.all.map { |offer| [offer.title, offer.id] } %> 
        <%= f.select :listing_id, options_for_select(offers_array) %>
    </div>
</div>