<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
    <h3 id="myModalLabel">Edit Profile</h3>
</div>

<%= form_for @profile, :html => { :multipart => true } do |f| %>
<div class="modal-body">
  <% if @profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@profile.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @profile.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class='span3 pull-left'>
	  <div class="field">
	    <%= f.label :quickPitch %>
	    <%= f.text_field :quickPitch %>
	  </div>
	  <div class="field">
	    <%= f.label :education %>
	    <%= f.text_field :education %>
	  </div>
	  <div class="field">
	    <%= f.label :location %>
	    <%= f.text_field :location %>
	  </div>
	  <div class="field">
	    <%= f.label :zipcode %>
	    <%= f.text_field :zipcode %>
	  </div>
          <div class="field">
	    <%= f.label :languages %>
	    <%= f.text_field :languages %>
	  </div>
  </div>
  <div class='span2 pull-left'>
	  
	  <div class="field" style='margin-bottom: 5px;'>
	    <%= f.label :about%>
	    <%= f.text_area :about, {:rows => '3', :style => 'width: 325px;'} %>
	  </div>
	  <div class="field">
	    <%= f.label :gender %>
	    <%= f.text_field :gender %>
	  </div>
	  <div class="field">
	    <%= f.label :birthdate %>
	    <%= f.text_field :birthdate %>
	  </div>
	  <div class="field">
	    <%= f.label :photo %>
	    <%= f.file_field :photo %>
	  </div>
  </div>
</div>

<div class="modal-footer">
    <button class="btn" data-dismiss="modal" aria-hidden="true">Cancel</button>
	<% if(current_user.id == 1 ||current_user.id == 2 ) %> 	
		<%= link_to 'Admin Panel', users_path, :class => "btn" %>
	<%end%>
	<%= f.submit 'Save', :class => "btn btn-primary" %>
</div>
<% end %>
