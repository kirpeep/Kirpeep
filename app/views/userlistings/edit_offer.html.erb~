<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
    <h3 id="myModalLabel">Edit Offer</h3>
</div>

<%= form_for @listing, :url => '/userlisting/' + @listing.id.to_s(), :method => 'put', :html => { :multipart => true } do |f| %>
<div class="modal-body">
	  

	  <div class="field">
	    <%= f.label :title %><br />
	    <%= f.text_field :title %>
	  </div>
	  <div class="field">
	    <%= f.label :description %><br />
	    <%= f.text_area :description  %><br />
	  </div>
	  <div class="field">
	    <%= f.label :inventoryCount %><br />
	    <%= f.text_field :inventoryCount %>
	  </div>
	  <div class="field">
	    <%= f.label :photo %><br />
	    <%= f.file_field :photo %>
	  </div>
	  <!-- add listing type -->
	  <div class="actions">
	    
	  </div>

	
	<div class="delete">
	    
  	</div>
</div>
<div class="modal-footer">
    <button class="btn" data-dismiss="modal" aria-hidden="true">Close</button>
    <%= f.submit 'Save', :class => "btn btn-primary" %> 
    <% end %>
    <%#= button_to "Delete", @offer, :method=>:delete, :class=>"destroy btn" %>
</div>
